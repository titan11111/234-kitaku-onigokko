# LEARNINGS — 234-kitaku-onigokko

## 2026-09-18 公開前にタップ偽陽性をゲーム側で潰した

`_tools/game-harness.sh` が 8項目中「タップ」だけ FAIL。canvas が DOM 先頭で、タイトル幕が全面を覆うため Playwright が canvas を叩けない（2026-09-14 と同じ）。

直し（241-shichirin-sanma / audit-calibration 2026-09-16 と同じ）:
1. `#overlay` を `<canvas>` より前に移動。見た目は `#game{z-index:0}` / overlay `z-index:20` で維持
2. タイトルの `TAP` を `<p>` から `<button type="button">` にした

再実行: **8/8 PASS**（`docs/harness-reports/234-kitaku-onigokko-2026-09-18T11-38-44-113Z.md`）。

**幕付きタイトルは偽陽性で片付けない。canvas が先頭なら幕を前へ置けば直る。**

---

## 2026-09-17 面が変わっても「2 / 5」では気づかない

HUD左上の数字は常時出ているので、変化として読めない。交番到達の瞬間は視線が街のほうにある。

直し: 画面中央に `STAGE`＋大きな数字を出し、**blur 16px→0 で浮かび、またぼやけて上へ消える**（2.6秒）。`pointer-events:none`。操作もポーズも奪わない。`textContent` のみ。

**状態の常時表示と、変化の演出は別物。** 常時HUDを大きくしても「今変わった」にはならない。

---

## 2026-09-17 鬼を2倍にした。当たりは2倍にしていない

見た目の `scale=2` と捕獲・衝突を同じ倍率にすると、通路（幅10m）が詰まり、リーチ3mで理不尽になる。

やったこと: メッシュと影だけ2倍。衝突半径 0.5→0.72。捕獲 1.5→2.0（隠れ中 1.0→1.35）。速度と視界は触らない。歩幅の見た目に合わせて脚の周期だけ落とした。

**大きい敵は「見える・当たる・速い」を全部上げると難易度が立方に跳ねる。** 見た目だけ先に上げ、捕獲はモデルに食い込む寸前まで。

---

## 2026-09-17 鬼が近いとBGMが1.1倍

近づいたときだけ上げ、離れたら戻す。連続補間ではなく **20m入／22m出** の2値。気配HUDと同じ距離なので、画面の警告と音の膨らみが同時に来る。

iOSは media要素の `.volume` を無視するので、既存の `bgmGain` に `BGM_VOL * 1.1` をフェードする。毎フレーム書き換えず、状態が変わったときだけ 0.28秒。ミュート中・停止中は触らない。

---

## 2026-09-16 交番ゴールは1回クリアだった。5ステージに直した

チェック結果: 交番メッシュと `kd<4.2` の到達判定はあった。ただし到達で即 `clearGame()`。ステージは無く、鬼は赤1体＋75秒後の青1体だけだった。

直し: 到達でステージ+1、交番を62m以上先へ移す、次の色の鬼を足す。5でクリア。ナビグリッドは交番移動のたびに `rasterize()` し直す。

---

## 2026-09-16 タイトル／エンディングをパンク×冷たい恐怖へ

タイトルからあらすじ・操作表・警告テロップを外した。見出しを「帰宅／鬼ごっこ」の2段にして、説明の代わりにスタンプ（LAST TRAIN / NO EXIT）だけ残した。エンディングも同じトーンで、成績と一言以外は置かない。

**タイトル画面に説明を書くと、怖さが説明になる。** 操作はパッドとプレイ中HUDに任せ、幕はポスターにする。

---

## 2026-09-16 BGM Behind_The_Locked_Gate

### やったこと

Suno由来の MP3（192kbps / 4.0MB / 2分53秒）を AAC-LC 96kbps の `audio/Behind_The_Locked_Gate.m4a`（2.0MB）に変換し、`<audio>` でループ再生。元MP3はフォールバックとして `audio/` に残した。ルート直下には置かない。

### 学び

**native ffmpeg の HE-AAC（`aac_he`）は使わない。** 236 で `afplay` が `'wht?'`、チャンネル数 0 になった。今回は `-c:a aac -b:a 96k`（AAC-LC）で `afinfo` が Stereo 2ch・173秒を返した。

**`new Audio()` は使わない。** タグ＋ `preload="none"` ＋ジェスチャ内 `play()`。iOS は media要素の `.volume` を無視するので、音量は `createMediaElementSource` → gain。ミュートは `pause()`。

**decodeAudioData に 4MB を載せない。** iPhone で展開が重い。`<audio>` ストリームのまま流す（237 と同じ）。

---

## 2026-09-16 携帯HUD：スタミナ／気配が主人公と重なる

### やったこと

タッチ時のスタミナ／気配バーを、スティック避けの「画面下から170px上」から **TIME直下の左上** へ移した。

### 学び

**パッド避けでHUDを“少し上”に逃がすと、三人称カメラの主人公位置に刺さる。**
下端から170pxはスティックとは離れるが、lookAtが画面中央〜やや下の主人公に当たるため、バーが胴体に乗る。逃がし先は「操作の隣」ではなく **画面の四隅（情報は上、操作は下）** まで持っていく。

---

## 2026-09-14 タイトル／エンディングのサイバーパンク化 ＋ コントロールパネル刷新

### やったこと

1. タイトル・ゲームオーバー・クリア・ポーズの4画面をサイバーパンク×ホラーで作り直し
2. 仮想コントローラを Pointer Events ＋ `setPointerCapture` で作り直し（視点◀▶を追加）
3. ミュート／明示ポーズ／最速記録の保存（`tg.234.*`）を新設
4. iOS鉄則（`/ios-game-base`）をまとめて適用

### 検証の証跡

- Playwright WebKit 390×844・hasTouch の自作スクリプトで **17項目 ALL PASS**
  （幕の確認／タップ開始／スティック追従・保持・解放／ダッシュ press／ミュートON・OFF＋localStorage／
  　ポーズ表示・再開／Pキー／ゲームオーバー画面／クリア画面／BEST保存／タイトルのBEST表示／JSエラー0件）
- `_tools/game-harness.sh` は 8項目中7項目PASS、**「タップ」のみFAIL＝既知の偽陽性**（下記）

### 学び

**1. harness の「タップ」FAILは、幕付きタイトル構成では構造的に必ず出る（2件目）**
`game-harness.mjs:189` は `button,[role=button],canvas` の **DOM順1番目**を叩く。
このゲームの1番目は `<canvas id="game">` だが、起動時は `#overlay`（タイトル幕）が全面を覆うため
Playwrightのhit-targetチェックが通らない。`elementFromPoint(canvas中心)` は `DIV` を返した＝幕が原因。
実際のタップは通る（タップ後に `#overlay` の display が `none` になることを実測）。
→ ゲーム側を触らず、`docs/audit-calibration.md` に追記した。**採点基準は書き換えない。**

**2. 「オーバーレイの上に置くボタン」はz-indexを最初に決めないと死ぬ**
ミュート／ポーズを `z-index:7` で置いたところ、`#overlay`（z-index:20）に覆われて
タイトル・ポーズ中に押せなくなった。**設定系ボタンは幕より前面（30）に置く**。
「ポーズ中にミュートしたい」は実際に一番起きる操作なので、ここを外すと機能が無いのと同じになる。

**3. パッドを足すとHUDと必ず衝突する。HUDの逃がし先をセットで決める**
スティック（左下）はスタミナ／気配バーと、アクションボタン（右下）はMAPと重なった。
`body.touchmode` を付けてタッチ時だけ HUD を退避させた（スタミナは上へ、MAPは右上へ）。
**パッドは「置く場所」ではなく「何をどかすか」までが1セット。**

**4. グリッチ表現は"常時わずかにずらす"。アニメーションだけに頼ると静止画で消える**
`::before/::after` のRGBずれをキーフレーム内だけで動かしたら、スクリーンショットでは素の文字に見えた。
ベースの `transform` に ±2px の恒常ずれを入れ、キーフレームでは跳ね幅を足す形へ変更。
X投稿用キャプチャを撮る前提なら、**静止画で成立するかを必ず1枚撮って確認する**。

**5. 日本語UIは390px幅で必ず折り返しを確認する**
`letter-spacing:.32em` の英字サブタイトルが「T O N I G HT」で割れ、操作表も1行ずつ折り返していた。
`clamp()` で字送りとフォントサイズを可変にし、操作表の文言を短縮して解消。
**幅390pxの実キャプチャを見るまで「入っている」と判断しない。**
